---
name: urban-novel-reviewer
version: 3.9.0
description: 网文综合审稿技能 v3.9.0（男频/女频全题材，21大类）。六大自动化审查+平台合规红线门禁+系统文/性转/伏笔/公路克苏鲁四大专项。v3.7 三级更新分级机制默认轻量模式减 60%~75% token。v3.9 轻量记忆索引体系（实体注册表/分层人物库/倒排索引/滚动摘要/题材缓存路由），超长篇审稿再省 50%+ 回溯 token。适用于审稿/校对/改文/润色/大纲把关/查AI味/OOC/人设/伏笔/文风/合规等场景。
---

# 网文综合审稿官（Urban Novel Reviewer）

## 前置强制执行：数据路径约定（优先级最高）

本技能采用**全局逻辑 + 项目级独立数据**架构，所有路径规则必须严格遵守：

1. **全局资源目录**：本技能自带的规则、模板、基准库存放于技能安装目录下的 `references/`，全局共享，只读不写。
2. **项目专属数据库**：统一存放于**当前项目根目录 `$WORKSPACE_FOLDER/.review-db/`**，每本小说独立一套，自动读写，数据完全隔离。
3. **路径基准**：所有相对路径均以 `$WORKSPACE_FOLDER`（当前项目根目录）为基准，禁止以技能自身目录为基准。
4. **自动初始化**：触发审稿时先检测项目根目录是否存在 `.review-db`；不存在则自动创建标准目录结构，无数据时自动降级为会话内临时模式。

### 项目数据库标准结构（v3.9.0 索引增强版）

```
$WORKSPACE_FOLDER/.review-db/
├── meta.json              # 项目元数据：书名、题材、cached_genre 路由缓存、当前章节（极轻量，<100token）
├── setting.md             # 核心设定：世界观 + 常驻人物卡（主角+核心配角）+ 力量体系，~300行内
├── project_state.json     # 全专项状态合并：system_state / gender_state / road_state / san_value
├── entity_index.json      # 【v3.9.0】实体注册表：人物/物品/势力/地点名片，检索总入口
├── characters/            # 【v3.9.0】分层人物库：重要配角独立建档（出场≥3章），出场才读
├── keyword_index.json     # 【v3.9.0】倒排索引：关键词→章节列表，伏笔定位/事件回溯
├── story_summaries.json   # 【v3.9.0】滚动摘要：最近10章详情+arc卷级摘要+全局简介（总量恒定~200行）
├── foreshadow/
│   ├── index.json         # 伏笔轻量索引：FID+状态+章节，日常仅读这个
│   ├── details/           # 单条伏笔详情，按需读写
│   └── archived.json      # 已回收伏笔归档，日常不碰
└── style_fingerprint.json # 仅保留文风指纹，其余向量临时计算不持久化
```

> **v3.9.0 索引说明**：
> 1. **确定性查表替代语义检索**：90% 的一致性核查是"某人物/物品/伏笔在哪章、什么状态"的确定性查询，结构化索引精确命中，比向量化轻一个数量级；
> 2. **常驻加载恒定**：meta + entity_index + foreshadow/index + setting 涉及段落 + recent 摘要 ≈ 300-500 行，不随章节数膨胀；
> 3. **索引轻量维护**：全部增量追加（实体计数 +1 / 关键词追加 / 摘要追加），纳入三级更新分级，轻量级模式自动维护，无需用户指令；
> 4. 旧库（v3.7/v3.8 结构）不强制迁移，审稿时惰性补建索引，下次审稿生效。

---

## 〇、输入解析与意图路由规则（前置执行，优先级最高）

**本章节优先级最高：执行任何审稿操作（含下文六大支柱、六步工作流、大纲审稿分支）之前，必须先完成本解析流程。** v3.1 新增、v3.2 扩展。作者的输入常常是短语式、省略式、口语化的（如"第三章 OOC""这段 AI 味""改商战""下一章也来一遍""帮我把下大纲"），不得因信息不全就直接开口追问或按全维度硬审——先按下述流程把碎片化输入转换成标准化「审稿任务单」，再路由到对应审稿模块。

### 第1步：语义补全（四维度，按优先级继承）

提取用户输入中的全部关键词，对照文末「附录：审稿意图词典」识别意图，缺失要素按以下优先级补全：

| 缺失维度 | 补全规则 | 示例 |
|---------|---------|------|
| 审稿对象 | ① 优先取对话上下文中最近审过/提到的文本或章节；② 用户本轮粘贴/选中了文本则以该文本为准；③ 均无则默认"用户当前提供的全部文本"；④ 用户只说章节号（"第三章"）时，到项目章节目录定位该章文件，定位不到再询问 | "查 OOC" → 审查当前文本的人物 OOC 问题 |
| 审稿维度 | 匹配意图词典对应九大能力；无维度关键词时默认「全维度基础审稿」 | "第三章" → 按默认标准全维度审查第三章 |
| 审稿深度 | 默认「标准深度」；出现"细查/深扒/逐句/好好看看"→深度；出现"简单看看/快速过一下/大概看下"→快速 | "简单看下合规" → 快速审查当前文本的平台合规性 |
| 输出要求 | 默认「问题清单 + 修改建议」；出现"只说问题/别给建议"→仅问题清单；"打分/多少分"→量化评分；"简要/一句话"→简要总结 | "AI 味 打分" → 检测 AI 味程度并量化打分 |

### 第2步：上下文继承（多轮对话默认继承，除非用户显式更改）

- 继承上一轮的**审稿标准**（番茄标准 / 起点标准 / 自建 C1-C6 红线等）；
- 继承上一轮的**审稿维度**（如上一轮重点查 OOC，这一轮说"下一章"自动继续重点查 OOC）；
- 继承上一轮的**审稿对象锚点**（如上一轮审第二章，这一轮"下一章/第三章"自动顺延章节定位）；
- 继承上一轮的**输出格式**（如上一轮要了分点输出/打分，这一轮默认保持）。

> 示例：上一轮"用番茄标准审第二章，重点查 OOC" → 这一轮"第三章也来一遍"
> 补全为：「用番茄平台标准，全维度审查第三章，重点核查人物 OOC，输出格式同上一轮」

### 第3步：生成标准化审稿任务单（内部执行，不必全文展示给用户）

解析完成后先在内部形成任务单，字段如下：

- 【审稿对象】：具体章节/段落/文件范围（含定位方式）
- 【核心维度】：对应九大审稿能力的 1 项或多项（全维度 / 指定维度）
- 【执行标准】：番茄 / 起点 / 自建红线 / 默认标准
- 【审稿深度】：深度审 / 标准 / 快速
- 【输出要求】：问题清单 / 修改建议 / 量化打分 / 简要总结
- 【特殊要求】：用户额外指定的约束（如"只看对话""别改剧情""模仿我文风"）

轻度模糊时（仅缺 1 个维度且默认值明确），直接按默认执行，并在报告开头一句话说明执行前提，例如：`📌 本次按番茄标准、全维度审查你粘贴的文本；只要查某一项随时说。`

### 第4步：路由执行

按任务单【核心维度】调度下文对应模块，**不改动原有审稿逻辑**：

| 任务单核心维度 | 路由到 |
|--------------|--------|
| 人物逻辑 / OOC | 六步工作流第3步 character-logic-rules（§3 含人情账本审计，v3.8.0 已并入原 social-audit-checklist 15 项清单）；维度 b) |
| AI 味检测 | ai-flavor-rules；维度 f)/A3 |
| 对话优化 | dialogue-game-rules；维度 c)/C4 |
| 剧情逻辑 / 伏笔数值 | character-logic-rules + 索引层伏笔/数值比对（临时提取，v3.7.0 起不落盘）；维度 a)/D2 |
| 商战 / 人情世故 | character-logic-rules §3 人情账本审计 + 都市商战矩阵；维度 D1 |
| 合规审查 | platform-gate-rules（G0 红线 C1-C6 + G1-G6）；维度 e) |
| 文风校验 | style-memory-db.md 文风指纹比对；A3 联动 |
| 模板套路检测 | template-cliche-rules；维度 d) |
| 大纲 / 卷纲 / 章纲 / 设定集 | **大纲审稿分支**（第三章，outline-review-rules），不走正文六步流程；命中系统文要素叠加 system-novel-rules |
| 系统文专项（设定/大纲） | system-novel-rules：四大维度 + 21 类赛道模板（主流 8 + 细分 13，v3.6.0 新增 #21 公路求生·克苏鲁向）+ 12 套性格人设模板，机制/人设**双校验**；随大纲审执行 30/100/大结局三节点前瞻推演；公路求生题材自动与 #4 末世求生、#16 诡异克苏鲁三重叠加 |
| 性别转变专项（性转/变嫁/女装大佬等） | gender-transition-rules：心理/行为/生理/社会身份/核心人设/剧情价值六大维度 + 6 套转变过程模板库；正文联动 C1/C2/C4；强制型性转叠加 system-novel-rules；大纲审分支叠加使用 |
| 伏笔追踪专项（预埋/回收/冲突/台账，v3.4.0；v3.6.0 扩充公路类） | foreshadow-judgment-rules + foreshadow-conflict-rules + foreshadow-topic-templates：六大类 27 小类判定（v3.6.0 新增 F23-F27 公路场景专属伏笔）+ 全生命周期状态机 + 四类冲突校验 + 遗忘预警阈值 + 12 类分题材伏笔模板（v3.6.0 新增公路求生·克苏鲁向）；正文审随第1步台账读取与第4步 D2 联动，大纲审分支叠加节点规划校验；深度档位 quick/full/precise 直接继承审稿深度；可与系统文/性转/大纲专项任意叠加 |
| 全维度审稿 | 六步工作流完整执行（门禁前置 + 12维 + 题材专项 + 向量库 + 文风） |

#### 第4.1节 · 三层路由架构兼容说明（v3.3.1）

本技能采用**三层路由架构**，性转 / 变嫁专项以「插件式增量接入」方式挂载，**零侵入原有核心逻辑、仅做增量审查、不替换通用审查**，全链路如下：

```
用户输入
  → 原有语义解析层（统一入口，100% 复用 v3.1 第〇章四步解析）
    → 通用审稿内核（原有六大支柱 + 正文六步 / 大纲分支两条流水线，核心算法与阈值不动）
      → 专项增强层（性转 / 变嫁模块：仅在通用审查完成后追加细分维度与附录结论）
        → 统一输出层（复用原有报告格式，命中性转时仅在末尾追加【专项审查附录】）
```

**v3.4.0 伏笔追踪专项接入说明（同架构增量扩展）**：伏笔追踪专项以完全相同的插件式方式挂载到专项增强层，成为第三个核心专项模块——专项增强层扩展为「系统文专项 + 性别转变专项 + 伏笔追踪专项（新增）」，三者可任意叠加触发（叠加优先级按 §4.2 四级排序，伏笔属第四级细分专项，不覆盖任何原有判定）；统一输出层在命中时于【专项审查附录】下追加伏笔追踪子段。伏笔专项全链路复用第〇章四步解析（触发词追加至既有意图词典）、六大支柱（复用对接见 §专项兼容层·伏笔追踪）、统一输出格式，**用户无感知切换，入口与交互与原有审稿完全一致**。

**v3.6.0 公路求生·克苏鲁向题材适配说明（既有专项的题材内容增量，不新增专项层）**：本次适配不新增第四类专项模块，而是向三个既有模块增量注入题材内容——① 系统文专项赛道模板库新增 #21 公路求生·克苏鲁向（载具/节点/资源/SAN/公路规则/任务奖励六维），命中公路要素时与 #4 末世求生、#16 诡异克苏鲁**三重叠加**；② 伏笔专项判定标准新增第六大类 F23-F27（公路场景专属伏笔），公路题材下与 F20-F22 同为 precise 档逐句扫描重点，分题材模板自动挂载公路行；③ 通用人物逻辑规则包新增 §1.6 公路节点节奏模型（C1 节奏维度）与 §2.6 SAN 值四级污染行为校验（C2 人物维度，分级内行为偏差豁免 OOC）。三处增量均自带【适用范围】门控，仅在「公路/载具/行车 + 求生/末世/系统 + 克苏鲁/诡异/SAN」要素组合命中时激活；未命中时规则休眠、加载与输出与 v3.5.0 完全一致。

#### 第4.2节 · 路由优先级排序与双重触发校验（v3.3.1，防误触与冲突）

所有专项按固定优先级路由，**低优先级专项永远不覆盖高优先级判定结果**：

1. **第一级：正文 / 大纲 / 设定集 类型判断**（决定走哪条流水线）
2. **第二级：主题材匹配**（都市 / 玄幻 / 末世 / 古言 / 无限流等）
3. **第三级：通用专项匹配**（系统文等已有专项）
4. **第四级：细分专项匹配**（性别转变 / 变嫁等细分专项；v3.4.0 起伏笔追踪专项同为第四级细分专项，与性转并列、可叠加，两者互不覆盖）

性别转变专项启用前需通过**双重触发校验**（单一关键词不启用，避免误触普通审稿）：
- **条件 1**：命中性转类关键词（参见 §一、触发条件，以及附录「审稿意图词典」性别转变专项类）
- **条件 2**：上下文确认为性转题材（出现性别转变情节、人物设定包含性别转换、作者明确要求性转专项审）—— 与「上下文确认题材匹配」双重校验一致，缺一不可。

**自动降级机制（v3.3.1）**：当题材识别的性转置信度 **< 70%** 时，自动退回**通用人物审稿**（仅按 character-logic-rules 标准 OOC 逻辑审查），不加载 gender-transition-rules，避免误判。

**手动开关指令（v3.3.1，与既有专项格式一致）**：
- 「启用性转专项审查」——后续审稿强制加载 gender-transition-rules（在双重触发基础上强开）
- 「禁用性转专项，按通用标准审」——后续审稿即使命中性转关键词，也仅按通用人物审稿处理

**伏笔追踪专项触发与档位规则（v3.4.0）**：
- **关键词激活深度档**：识别到伏笔类关键词（伏笔 / 埋线 / 挖坑 / 填坑 / 回收 / 线索 / 闭环 / 剧情呼应 / 查伏笔 / 排查伏笔 / 伏笔检查 / 回收核对 / 剧情复盘 / 伏笔忘收 / 剧情吃书 / 前后矛盾 / 线索遗漏等，见附录意图词典「伏笔追踪专项类」）时自动激活伏笔专项深度检查；
- **默认基础检查（零额外负担）**：用户仅提「审稿」而不带伏笔关键词时，仍默认执行**基础伏笔检查**（预埋识别 + 状态推进 + 明显冲突标记，quick 档内容），不增加审稿负担、不显示专项附录（输出与原版本完全一致）；
- **自动叠加场景**：提交新章节正文审稿、大纲 / 卷纲 / 章纲专项审稿、全文逻辑自检、系统文 / 性转等专项审稿时，自动叠加对应题材的基础伏笔校验（分题材模板由 foreshadow-topic-templates 按主题材挂载）；
- **深度档位映射（无需用户单独设置）**：快速审稿 → quick（仅高风险遗漏与明显冲突）；标准审稿 → full（预埋识别 + 回收状态 + 冲突校验 + 基础台账）；深度审稿 → precise（全维度校验 + foreshadow_base 精度对比（如有）+ 完整追踪表）；
- **手动开关指令（与既有专项格式一致）**：「启用伏笔专项深度审查」——后续审稿强制按 precise 档执行伏笔校验；「禁用伏笔专项，按通用标准审」——后续审稿跳过伏笔专项（含基础检查）。

### 第4.5步：输入类型判断（v3.2 新增，决定走哪条流水线）

路由前先判断审稿对象类型：
- **规划文档类**（大纲/卷纲/章纲/设定集/世界观/系统设定，判定信号见 [outline-review-rules.md](references/rules-pack/outline-review-rules.md) §0.1）：路由到**第三章「大纲审稿专项流程」**，不进入正文六步工作流（不跑门禁 G1-G6、12 维评分、向量库比对、文风监控）；命中性别转变要素叠加 gender-transition-rules（v3.3.0）；伏笔追踪专项自动叠加「大纲 × 伏笔节点规划校验」（回收计划 / 回收节奏 / 长线布局合理性 / 伏笔负载评估，见 foreshadow-topic-templates §四，v3.4.0）；命中公路求生·克苏鲁向要素（载具/公路节点/行车规则/SAN 值）时叠加 system-novel-rules #21 赛道模板做前瞻推演（载具升级梯度/节点三要素/资源守恒/移动性保持，v3.6.0）；
- **正文类**（已成稿章节/粘贴正文）：走第二章六步工作流；成稿中含系统任务/奖励/数值/面板内容时，system-novel-rules 随第3步加载联动 C2/C3/D2，系统人格/语气突变或越界言行按 C2 判并对照 12 套性格人设模板做人设一致性检测（v3.2.1）；成稿中出现性别转变相关内容时，gender-transition-rules 随 character-logic-rules 加载，行为/心理穿帮与人格割裂判 C2、反差浪费/主线关联弱判 C4、设定不一/阶段跳步判 C1，强制型性转叠加系统文专项（v3.3.0）；正文审稿自动执行基础伏笔检查——第1步读取 `.review-db/foreshadow/index.json` 伏笔索引（v3.7.0 起唯一台账），第4步 D2 维度联动伏笔索引做增量校验，命中伏笔关键词或深度档时按 foreshadow-judgment-rules / foreshadow-conflict-rules 执行全链路校验（v3.4.0）；成稿命中公路求生·克苏鲁向要素时（v3.6.0）：system-novel-rules #21 随第3步加载并与 #4/#16 三重叠加（载具属性/资源守恒/奖励代价判 C3/D2），character-logic §1.6 公路节点节奏模型联动 C1（过密/过疏/失衡/移动性丧失四预警），§2.6 SAN 值分级行为校验联动 C2（分级内偏差不判 OOC、无诱因掉 SAN/秒回满/数值穿帮判 C2/D2），伏笔专项对 F23-F27 公路场景伏笔按题材高精度识别（full 档场景级、precise 档逐句）；
- **混合输入**（大纲 + 已写章节）：大纲走第三章、章节走六步流程，报告分区输出；
- 判断不清时按中度模糊处理，向用户确认"审大纲还是审已写章节"。

### 第5步：模糊需求分级澄清

- **轻度模糊**（缺 1 个维度、默认值明确）：直接执行，开头说明执行前提（见第3步）；
- **中度模糊**（缺核心审稿对象，或意图词指向 2 个以上维度无法消歧）：给出 ≤3 个精准选项请用户确认，例如：
  > 我理解你要审稿，请问重点查哪项？
  > 1. 人物 OOC 与人设一致性
  > 2. 文本 AI 味与模板化
  > 3. 剧情逻辑与伏笔穿帮
- **重度模糊**（输入与审稿完全无关，或既无文本又无章节可定位）：说明本技能能力范围（审稿/查AI味/查OOC/查合规/改对话等），引导用户提供章节文本或明确需求；
- **禁止事项**：禁止凭空猜测用户需求；禁止在无审稿对象时编造文本内容；禁止执行与审稿润色无关的操作。

---

网文创作的综合性质量控制工具。v3.1 在原有六大支柱前增加「前置语义解析层」（见第〇章），六大支柱审稿逻辑保持不变：

1. **记忆文档系统**——持续引用真相/设定文档验证一致性；
2. **审稿向量数据库**——每个项目独立库实例（`.review-db/`），剧情逻辑/OOC/对白口语化/人情账本四大检索比对；
3. **文风记忆向量库**——提取作者文风指纹，监控风格漂移、允许风格自然演变；
4. **模板库智能匹配**——内置 silver-novel-skills 全量模板（男频13题材/女频8题材 + 13通用审稿agent + 写作技法 + 社交人格卡/关系账本模板），按识别类型自动加载；
5. **离线规则包（v3.0 核心升级）**——九大技能核心规则已内化至 `references/rules-pack/`：AI 味检测（qu-ai-wei/说人话/humanizer/novel-audit/番茄五源合一）、对话博弈（对话博弈大师/social/story-review）、人物逻辑与利益校准（social/novel-audit/plot-tension）、模板套路检测（story-review 词库/novel-audit）、平台门禁与**合规红线**（番茄/story-review 三平台 rubric + 自建 C1-C6 红线清单）；本机技能在与不在都能审；
6. **外部技能调度**——本机已装技能存在时仍标注 `[调度:技能名]` 来源，缺失时自动使用内置规则包。

---

## §专项兼容层 · 底层能力复用机制（v3.3.1，六大支柱全量复用 + 增量扩展）

性转 / 变嫁专项是「人物 OOC 审查」下的子插件，**六项底层能力全部复用原六大支柱，只做增量扩展，不替换、不重写通用逻辑**。对应关系与增量扩展逐项如下：

| 支柱 | 复用（100% 保留） | 增量（v3.3.1 扩展，仅性转专项用） |
|------|-----------------|--------------------------------|
| 1. 记忆文档系统 | 同库校验，人物基础人设 / 世界观 / 剧情线仍走原有记忆库，不改动原有字段结构 | 在记忆文档的人物条目下新增 3 个扩展字段：`性别转变状态`、`当前阶段`、`核心转变节点`，用于校验转变前后的设定一致性；通用字段与扩展字段同库同源，不会出现两套人设 |
| 2. 审稿向量数据库 | 完全复用原有「剧情逻辑 / OOC / 对白口语化 / 人情账本」四大检索比对能力（v3.7.0 起比对指纹临时提取），基础人物逻辑问题仍由通用比对识别 | 在 `project_state.json` 的 `gender_state` 字段（v3.7.0 精简，原 `.review-db/gender-transition/` 子目录已并入）追踪 4 类专项特征状态：**心理阶段特征**（否认/抗拒/适应/接纳/整合）、**行为惯性特征**（旧性别习惯残留、新性别行为适配）、**身份认同特征**（自我认知、社会身份重构）、**变嫁情感特征**（排斥/动摇/接纳）；检索顺序：先跑通用特征 → 再叠加专项特征 → 统一排序，不出现两套判定标准 |
| 3. 文风记忆向量库 | 完全复用作者原有文风指纹，文风漂移基础判定标准、容差带、EWMA 演变机制不变 | 新增**性别场景文风校准规则**：仅校验「性转心理 / 生理描写时是否出现文风突变、是否过度偏离作者整体风格」，不改写原文风基准；场景性变化通过的记录为演变候选 |
| 4. 模板库智能匹配 | 完全复用原有「男频 13 题材 / 女频 8 题材 + 系统文全类型」主模板库，题材识别与基础标准不变 | 性转 / 变嫁模板全部设为「附加插件模板」，不替代主模板；**叠加顺序（主题材 → 系统模板 → 性转插件模板）**：先匹配主题材模板（如都市 / 末世 / 修仙）→ 再匹配系统模板（如有）→ 最后叠加性转专项模板，各模板负责各自维度互不冲突，基础标准永远以主题材模板为准 |
| 5. 离线规则包 | 完全复用原有九大核心规则（AI 味 / 对话 / 人物 / 模板 / 合规 / 人情 / 场景 / 大纲 / 系统文），基础审查标准不变；原有冲突仲裁顺序：记忆文档事实 > 作者声明 > 平台规则 > 专项方法论 > 通用规律 | 新增 gender-transition-rules 作为「人物逻辑规则」下的子规则集，仅在命中性转题材时激活；**冲突处理三原则**：① 通用与专项无交集时各自独立判定；② 判定重叠时「通用规则优先级高于专项规则」；③ 通用规则已判定为「问题」的内容，专项规则不再重复判定，避免双重扣分 |
| 6. 外部技能调度 | 完全复用原有外部技能调度机制与标注规范 | 性转专项模块调用技能时统一标注：`[调度:技能名 + 性转专项增强]`，与原有标注格式完全兼容，便于审阅者识别来源 |

**灰度级与兜底（v3.3.1）**：
- 性转专项规则默认设置为**建议级**，输出仅作为提示，不影响核心总分与 12 维评级；运行稳定后可升级为**问题级**，纳入核心评分框架。
- 专项数据（扩展字段、专项状态）存 `project_state.json` 的 `gender_state` 字段（v3.7.0 精简，原 `.review-db/gender-transition/` 独立子目录已并入合并），物理隔离不污染记忆库；专项特征向量改为审稿时临时提取、审完即弃。
- **模块级回退清单**：若专项规则出现问题，按以下三步可立即回退至 v3.2.1 功能集、原有审稿不受任何影响：① 删除 `references/rules-pack/gender-transition-rules.md`；② 删除 `references/rules-pack/INDEX.md` 中 gender-transition 对应条目、调用接口 8–10 条兼容条款；③ 删除本 §专项兼容层、审查模式表与意图词典中性别转变专项内容。

**上下文继承兼容**：性转专项参数（性别转变阶段、人物状态、是否启用/禁用专项）完全复用 Trae 原生上下文机制，跨轮继承——与上一轮的审稿标准、维度、深度、输出格式一起继承，无需单独开发逻辑。

---

## §专项兼容层 · 伏笔追踪专项复用机制（v3.4.0，六大支柱全量复用 + 增量扩展）

伏笔追踪专项是「剧情逻辑审查」下的深度增强子插件（与性转专项同级的插件式接入），**六项底层能力全部复用原六大支柱，只做增量扩展，不替换、不重写通用逻辑、不建独立数据库**。对应关系与增量扩展逐项如下：

| 支柱 | 复用（100% 保留） | 增量（v3.4.0 扩展，仅伏笔专项用） |
|------|-----------------|--------------------------------|
| 1. 记忆文档系统 | 同库校验，世界观 / 人物 / 体系设定 / 剧情摘要仍走 setting.md，不改动原有字段结构；`setting_doc` / `global_history` / `novel_meta` 等伏笔技能参数全部自动复用记忆文档既有数据，**零额外输入** | 在项目数据库（`.review-db/foreshadow/` 独立子目录，v3.7.0 起唯一台账）新增 `index.json` 轻量索引（FID / 分类 / 状态 / 优先级 / 锚点 / 计划回收章）+ `details/Fxxx.json` 完整详情：跨章节自动继承（每章审后增量更新，下一章第1步自动读取）；设定联动校验直接调用 setting.md 世界观 / 人物设定，无需重复输入；兼容旧库 hooks.jsonl 三状态映射（planted/reinforced→open，half-closed→half，closed→closed，overdue/broken 加标记），不出两套账 |
| 2. 审稿向量数据库 | 完全复用原有「剧情逻辑 / OOC / 对白口语化 / 人情账本」四大检索比对能力（v3.7.0 起比对指纹临时提取、审完即弃），基础因果断裂 / 事实矛盾仍由通用比对识别 | 审稿时临时提取「伏笔特征」5 类（v3.7.0 起不持久化）：**实体类伏笔特征**（F1-F6）、**人物类伏笔特征**（F7-F11）、**力量体系类伏笔特征**（F12-F15）、**剧情长线类伏笔特征**（F16-F19）、**隐性弱埋线特征**（F20-F22）；检索顺序：先跑通用特征 → 再叠加伏笔专属特征 → 统一排序，通用剧情检索能力 + 伏笔专属特征 = 远超单技能识别精度 |
| 3. 文风记忆向量库 | 完全复用作者文风指纹、风格漂移判定标准、容差带与 EWMA 演变机制，不改写原文风基准 | 新增**伏笔自然度校验规则**：判断伏笔是否符合作者整体文风——伏笔句叙述腔突变 / 修辞密度突增判「伏笔伪装度不足」（🟡 生硬提示）；明示型伏笔密度 >2 处/千字判过度直白；场景性变化通过的记录为演变候选，不判罚 |
| 4. 模板库智能匹配 | 完全复用现有题材模板匹配机制（genre-classifier → templates/），主题材模板的基础标准（叙事节奏 / 爽点密度）不变 | 伏笔分题材模板（foreshadow-topic-templates）设为「附加插件模板」，不替代主模板；**叠加顺序扩展（主题材 → 系统模板 → 性转插件模板 → 伏笔分题材模板）**：加载主题材模板时自动挂载对应伏笔小节，不同题材采用不同伏笔合理性标准，避免一刀切误判 |
| 5. 离线规则包 | 完全复用原有九大核心规则（含 character-logic 剧情逻辑 / 设定冲突基础规则），基础审查标准不变；原有冲突仲裁顺序：记忆文档事实 > 作者声明 > 平台规则 > 专项方法论 > 通用规律 | 新增伏笔三规则包作为「剧情逻辑规则」下的**伏笔专项规则子集**：判定标准（六大类 27 小类，v3.6.0 含公路场景专属类 F23-F27，foreshadow-judgment-rules）+ 冲突校验（设定 / 人物 / 时间线 / 剧情自洽四类，foreshadow-conflict-rules）+ 分题材模板（foreshadow-topic-templates），仅命中伏笔需求或自动叠加场景时激活；**冲突处理三原则**：① 通用与专项无交集时各自独立判定；② 判定重叠时「通用规则优先级高于专项规则」（通用剧情规则优先级 > 伏笔专项规则）；③ 通用规则（含 D2 伏笔索引超期 / 矛盾判定）已判定为「问题」的内容，伏笔专项不重复输出，避免双重扣分 |
| 6. 外部技能调度 | 完全复用现有调度机制与标注规范 | 伏笔专项调用技能时统一标注：`[调度:技能名 + 伏笔追踪增强]`（对外标注 `[调度:伏笔追踪增强]`），与原有标注格式完全兼容，无需单独开发调度逻辑 |

**灰度级与兜底（v3.4.0，与 v3.3.1 模式一致）**：
- 伏笔专项默认设置为**建议级**，输出仅作为提示与预警（伏笔预警 / 回收时机建议），不影响核心总分与 12 维评级；运行稳定后可升级为**问题级**；
- **灰度运行**：默认仅开启基础伏笔检查（quick 档内容），深度校验与完整台账需用户命中关键词或深度档触发，不降低原有审稿速度；
- 专项数据（索引、详情）全部存在**独立子目录**（`.review-db/foreshadow/`），与记忆库物理隔离（v3.7.0 起伏笔特征临时提取、性转专项状态并入 `project_state.json`，不再有独立向量子目录），不污染任何现有数据；
- **模块级回退清单**：若伏笔专项出现问题，按以下三步可立即回退至 v3.3.1 功能集、原有审稿不受任何影响：① 删除 `references/rules-pack/foreshadow-judgment-rules.md`、`foreshadow-conflict-rules.md`、`foreshadow-topic-templates.md` 三个文件；② 删除 `references/rules-pack/INDEX.md` 中伏笔三包对应条目与调用接口兼容条款；③ 删除本节伏笔复用机制、第〇章伏笔路由与触发规则、审查模式行、意图词典「伏笔追踪专项类」、铁律第 10 条专项通用条款；
- **可选依赖**：`foreshadow_base` 为可选参数，用户未传入原生系统输出时自动跳过精度对比环节，不影响任何核心功能。

---

## 一、触发条件

- 用户说"审稿""审一下""审一下这章""全面检查""帮我校一遍""这章质量怎么样""把关"
- 用户要求查"OOC""人设崩没崩""人物不对劲""数值对不对""伏笔有没有忘""和设定矛盾吗"
- 用户要求查"AI味""去味""太假了""不像人写的""生硬""模板化""对话自不自然""台词尬""人情世故对不对""商战假"
- 用户要求查"文风""风格变了""文风飘了""是不是同一个人写的""文风漂移"
- 用户要求查"违规吗""能过吗""番茄能过吗""查敏感""查红线"
- 用户说"改一下""优化""润色""改对话""调剧情"等针对已有文稿的修改请求
- 用户粘贴章节或指定章节文件要求多章对比、连载一致性审查
- 用户给出碎片化/省略式指令（"第三章 OOC""这段 AI 味""下一章也来一遍""简单看下合规"）——由第〇章前置解析层补全后执行
- 用户要求审"大纲/卷纲/章纲/设定集/世界观/开篇设定/剧情框架"（v3.2 大纲审稿分支）
- 用户要求审"系统设定/系统规则/任务奖励/签到机制/数值膨胀/金手指设定"（v3.2 系统文专项，大纲阶段为主）
- 用户要求审"系统文细分赛道设定"：反派掠夺/直播弹幕/马甲/轮回回档/词条编辑/养崽/学霸/克苏鲁诡异/宗门建设/美食食神/军阀争霸/赘婿等小众系统赛道，或查"系统人设/OOC/系统崩人设/系统类型题材违和"（v3.2.1 扩充，走双校验 + 三节点前瞻推演）
- 用户要求审"性转/性别转变/变身/男变女/女变男/TS/女装大佬/伪娘/男娘/变嫁/嫁人/女体化/男体化/系统强制变身/诅咒变性/慢慢变成女生/慢慢变成男生"等性别转变类题材（v3.3.0 性别转变专项 / v3.3.1 兼容升级：需双重触发校验 + 置信度 <70% 自动降级，支持手动开关），或查"性转人设崩没崩/心理逻辑合不合理/变嫁铺垫够不够/女装大佬违和感"
- 用户发送手动开关指令（v3.3.1）：「启用性转专项审查」 / 「禁用性转专项，按通用标准审」
- 用户要求查"伏笔/埋线/挖坑/填坑/回收/线索/闭环/剧情呼应"等伏笔追踪类需求（v3.4.0 伏笔追踪专项）：通用类触发词自动激活深度档；动作类（查伏笔/排查伏笔/伏笔检查/回收核对/剧情复盘）与问题类（伏笔忘收/剧情吃书/前后矛盾/线索遗漏）同属此列
- 用户提交新章节正文审稿、大纲/卷纲/章纲专项审、全文逻辑自检、系统文/性转专项审——自动叠加对应题材的基础伏笔校验，无需用户明确说"查伏笔"（v3.4.0 自动叠加触发）
- 用户发送手动开关指令（v3.4.0）：「启用伏笔专项深度审查」 / 「禁用伏笔专项，按通用标准审」
- 用户明确说"用都市审稿技能审一下"

不触发：纯创作新章节（交给写作技能）；不针对任何文稿/大纲的纯剧情构思、脑洞闲聊（可正常对话，但不启动审稿流程；用户拿出大纲/设定文档要求把关时触发大纲审）。本技能已支持男频/女频全题材；非都市题材（玄幻/古言/科幻等）以 silver 题材模板 + 通用审稿 agent 为主，都市矩阵降权。

---

## 二、审稿工作流（前置解析 + 六步，不得跳步）

> **进入本工作流前，必须已完成第〇章「输入解析与意图路由规则」**：任务单的【核心维度】决定第3步加载哪些规则包、第4步执行哪些审查；【审稿深度】决定报告详略；【输出要求】决定第6步报告形式。下文六步为审稿执行主体，不得跳步。

---

### 数据加载执行流程（严格控 token，前置于第1步）

采用**三步按需加载机制**，极致控制 token 消耗，避免全量加载无关数据：

#### Step 0：启动预检（轻量，<100 token）

读取 `./.review-db/meta.json`，仅获取项目基础信息与模块标记（书名、题材、**cached_genre 路由缓存**、当前章节、已启用模块标记）。

- **无 meta 文件**：视为新项目，执行最小化初始化，创建标准目录结构（按"前置强制执行"章节定义）；
- **有 meta 文件**：根据当前任务类型，按需加载对应模块数据（见 Step 1）。

#### Step 1：模块级按需加载（仅加载当前任务涉及的数据）

**常驻索引层（每次审稿必读，~200 行）**：

| 数据 | 用途 |
|------|------|
| `./.review-db/entity_index.json` | 实体注册表：本章出场人物/物品/势力的名片与定位（常驻/建档/名片层） |
| `./.review-db/foreshadow/index.json` | 伏笔轻量索引（全局伏笔状态速览） |
| `./.review-db/story_summaries.json` 的 recent_chapters | 最近 10 章摘要（承接上文） |

**按任务类型叠加**：

| 任务类型 | 加载数据 |
|---------|---------|
| 基础审稿（AI味/OOC/对话/逻辑/合规） | `./.review-db/setting.md` 涉及段落 + 本章出场常驻人物卡；**出场配角在建档层时按需读 `characters/Cxxx.md`（每人~40行），名片层临时提取** |
| 系统文专项审查 | `./.review-db/project_state.json` 中 `system_state` 字段 + 对应题材模板 |
| 性别转变专项审查 | `./.review-db/project_state.json` 中 `gender_state` 字段 + 对应模式模板 |
| 伏笔追踪专项审查 | `./.review-db/foreshadow/index.json`（仅索引，不加载详情） |
| 公路求生/SAN值 | `./.review-db/project_state.json` 中 `road_state` / `san_value` 字段 |
| 文风校验 | `./.review-db/style_fingerprint.json`（仅文风指纹） |
| 跨章核查（伏笔回收/事件冲突/剧情连贯） | `./.review-db/keyword_index.json` 定位章节 → `story_summaries.json` 对应摘要核对，**禁止直接回溯正文全章** |

**未触发的模块完全不读取**，节省 token。多个专项可叠加触发，从 `project_state.json` 一次性读取对应字段。

#### Step 2：深度详情按需读取（单条文件，禁止批量全量）

仅当需要核对具体条目详情时，才读取对应单条数据文件：

- **伏笔详情**：仅读取对应 FID 的单条文件（如 `./.review-db/foreshadow/details/F001.json`），禁止批量加载全量 details/；
- **设定细节**：仅定位对应段落读取（如 setting.md 中"力量体系"章节），不读取全文；
- **人物卡**：常驻层查 setting.md；建档层按需读 `characters/Cxxx.md`；名片层从本章正文临时提取；
- **历史剧情**：当前 arc 之外的历史剧情查 `story_summaries.json` 的 arc_summaries，仍不够再回溯正文；
- **人物/剧情向量**：临时从正文和设定中提取，不持久化存储，审完即弃。

---

### 更新分级原则（默认执行轻量级，v3.7.0 核心优化）

**无用户明确指令时，默认执行轻量级更新，禁止自动修改章纲、卷纲及全量记忆文档。**
无明确指令，绝对不自动修改章纲、卷纲；修改正文时，改到什么范围，就更新对应范围的记忆数据，绝不全量联动。

#### 三级更新标准

| 更新等级 | 触发条件 | 更新范围 | 额外 token 消耗 |
|---------|---------|---------|--------------|
| **轻量级（默认）** | 用户仅提交正文修改 / 普通审稿，无额外指令 | 1. 更新 `meta.json` 当前章节号<br>2. 仅更新涉及到的伏笔状态（回收/新增）<br>3. 增量追加 `style_fingerprint.json` 文风指纹<br>4. **索引维护（v3.9.0，纯增量追加）**：`entity_index.json` 出场实体计数+1/新实体追加名片行 + `keyword_index.json` 追加本章 8-12 个关键词 + `story_summaries.json` 追加本章摘要（3-5 句，溢出章粗合并进 arc）<br>**绝对不碰**：章纲、卷纲、setting.md、project_state.json、characters/ 建档 | 极低（<150token） |
| **标准级** | 用户指令：`/sync-setting` / 「同步更新设定」 | 包含轻量级全部内容<br>+ 增量修改 `setting.md` 对应人物/世界观段落<br>+ 更新 `project_state.json` 对应专项字段<br>+ 人物卡建档（出场满 3 章的配角创建 `characters/Cxxx.md`）+ `entity_index.json` key_facts 增量更新<br>**仍不碰**：章纲、卷纲 | 中（仅修改涉及段落） |
| **全量级** | 用户指令：`/sync-outline` / 「同步更新章纲/卷纲」 | 包含标准级全部内容<br>+ 同步重写对应章纲<br>+ 联动修正卷纲剧情节点<br>+ arc 摘要精合并重写 + `global_summary` 重写 + entity_index 全量校验（死实体归档）+ 人物晋升/降级执行<br>+ 全量校验全局记忆一致性 | 较高（按大纲篇幅） |

#### 严格禁止的自动行为

1. **禁止**根据正文修改自动反向推导修改章纲、卷纲，必须用户主动发指令才执行；
2. **禁止**修改一小段正文就全量重写整个设定文档、整个状态库、整个索引文件；
3. **禁止**未触发对应专项时，更新该专项的记忆数据；
4. **禁止**为跨章核查直接回溯正文全章——先查 `keyword_index.json` 定位、再读 `story_summaries.json` 摘要，正文回溯仅限摘要无法回答的细节。

---

### 数据写入与持久化规则（增量写入，禁止全量重写）

所有更新严格执行「增量修改」，只改命中的行/条目，不重写整个文件：

1. **setting.md（设定文档）**：
   - 仅修改涉及的人物段落 / 世界观小节，其余内容原样保留；
   - 按标题定位，精准替换对应段落，不全文重写。
2. **foreshadow/index.json（伏笔索引）**：
   - 新增伏笔：仅追加新条目，不改动原有条目；
   - 状态更新：仅修改对应 FID 的状态字段，其余不动；
   - 归档：仅移动对应条目到归档文件，不重写整个索引。
3. **project_state.json（专项状态）**：
   - 仅更新对应字段，其余字段原样保留；
   - 未触发的专项，绝对不触碰对应字段。
4. **style_fingerprint.json（文风指纹）**：
   - 按 EWMA 指数滑动更新，仅修改量化字段值，不重写全文。
5. **entity_index.json（实体注册表，v3.9.0）**：
   - 出场实体仅更新 `last_ch` / `appear_count` 计数字段；
   - 新实体仅追加一行名片，key_facts 仅 /sync-setting 时更新；
   - 禁止全量重建注册表（死实体清理仅全量级执行）。
6. **keyword_index.json（倒排索引，v3.9.0）**：
   - 仅追加本章关键词到既有条目的章节列表，不重建索引；
   - 体积超限时按"单关键词保留最近 30 章、总量>500 清单次条目"规则裁剪。
7. **story_summaries.json（滚动摘要，v3.9.0）**：
   - 仅追加本章摘要到 recent_chapters；溢出章粗合并进 arc（拼接压缩）；
   - arc 精合并重写、global_summary 重写仅全量级执行。
8. **characters/（建档层人物卡，v3.9.0）**：
   - 建档仅 /sync-setting 执行（出场满 3 章触发）；
   - 更新仅修改涉及字段，按人物卡模板增量维护。
9. **已归档数据（archived.json）**：默认不触碰，仅手动指令触发归档操作。
10. **meta.json**：每次审稿后更新当前章节号，cached_genre 仅题材识别/漂移重识别时更新，其余字段仅在初始化时更新。

---

### 第1步：记忆加载 + 审稿库初始化（强制，先于一切审稿判断）

任务单【审稿对象】确定后执行。审稿前必须先建立"记忆底座"，不得只凭章节内文判断：

1. **定位记忆文档**。在项目根目录扫描以下文件（文件名模糊匹配，命中即用）：
   - 真相/记忆类：`*真相*`、`*记忆*`、`*truth*`、`*bible*`、`story_bible.md`、`current_state.md`、`pending_hooks.md`
   - 大纲类：`大纲/`、`*大纲*`、`*卷纲*`、`outline.md`
   - 设定类：`setting/`、`*设定*`、`character_social.md`、`relationship_map.md`、`social_scenarios.md`
   - 章节类：`正文章节/`、`章节/`、`chapter*.txt`
2. **打开/初始化项目审稿库**（按 [references/review-vector-db.md](references/review-vector-db.md) §1.2 执行，Step 0 已完成预检）：
   - 检查项目根目录 `.review-db/meta.json`；
   - **不存在 = 新项目**：自动创建独立库实例（含 v3.9.0 四大索引文件），从记忆文档首次灌入核心设定/实体名片/伏笔，报告开头注明 `🆕 已为《书名》初始化独立审稿数据库`；
   - **存在 = 旧项目**：校验项目ID一致后打开，严禁串库；旧结构库（无 v3.9 索引文件）惰性补建；
   - 文风库为单文件 `.review-db/style_fingerprint.json`（按 [references/style-memory-db.md](references/style-memory-db.md)），随项目库一并初始化/更新。
3. **建立本章索引锚点**。读取记忆文档 + 常驻索引层（`entity_index.json` 实体名片 + `foreshadow/index.json` 伏笔索引 + `story_summaries.json` 最近章节摘要，v3.9.0）后，按 [references/memory-index.md](references/memory-index.md) 提取与本章相关的：
   - 出场人物的五维档案、灵魂铁则、语言指纹、情绪反应库（**常驻层查 setting.md；建档层按需读 characters/Cxxx.md；名片层临时提取**）
   - 当前数值状态（系统点数/好感值/等级/货币等全部追踪表）
   - 进行中任务、持有物品、已解锁技能（**物品/技能状态先查 entity_index.json 的 items 条目**）
   - 待回收伏笔清单、已确认事实清单（不可打破项）
   - 大纲中本章节点的目标、节奏、出场角色、情感变化
4. **确认记忆文档更新状态**。若记忆文档进度落后于被审章节，在报告开头标注：
   `⚠️ 记忆文档滞后：文档进度=第X章，被审章节=第Y章，数值/伏笔类判断以正文回溯+伏笔索引为准，建议审后更新记忆文档。`
5. **读取全局伏笔索引（v3.4.0，v3.7.0 起由 index.json 承载）**。`.review-db/foreshadow/index.json` 存在时读取全局伏笔索引（FID / 六大类分类 / 状态机 / 计划回收章 / 优先级；v3.6.0 起含公路场景专属类 F23-F27），作为本章伏笔增量校验底座；不存在则本章审后首次建档。需要核对具体伏笔时按需读取 `details/Fxxx.json`，禁止批量加载（状态机与回写纪律见 [rules-pack/foreshadow-judgment-rules.md](references/rules-pack/foreshadow-judgment-rules.md) §一及 §四）。

记忆文档与库都缺失时：以用户提供的背景材料为基准；什么都没有时，只做文字/AI味/结构/文风维度审查，跳过一致性维度并在报告中声明。

### 第2步：题材智能识别与模板自动加载（v3.9.0：缓存优先路由）

按 [references/genre-classifier.md](references/genre-classifier.md) 执行，**缓存优先**：

0. **缓存快速路由（v3.9.0，第二次及以后审稿必走）**：读取 `meta.json` 的 `cached_genre`——
   - **有缓存且置信度 ≥80%**：抽取被审文本 5-8 个强信号词快速校验，命中 ≥3 个 → 直接按缓存的 templates/agents 清单加载，**跳过全量打分**（省 ~150-200 token）；
   - **无缓存 / 校验失败**：走下方完整识别流程，结果写入 `meta.json.cached_genre`；识别结果与缓存不一致时报告标注「⚠️ 题材漂移」并用新结果更新缓存；
   - 混合题材（双主类型权重接近）不缓存，每次走完整识别。
1. **频道判定**：男频/女频（信号表打分）；跨频道作品主流程走高得分分支，另分支加载核心专项。
2. **题材打分**：21 个候选题型（男频：都市日常/高武/异能/商战/娱乐圈/玄幻/仙侠/悬疑/穿越/游戏/科幻/历史/恐怖；女频：现代言情/古言/BL/GL/女尊/快穿/悬疑恋爱/女频仙侠）按强/中/弱信号加权计分，定主类型 + 辅类型。
3. **模板映射加载**：按映射表从内置 silver 模板库（`references/templates/`）Read 对应文件——1 个题材模板 + 命中专项模板 + 最多 2 个审稿 agent，总计 ≤ 5 个；混合题材叠加。
4. **矩阵激活**：都市四类子类型同时激活 [references/genre-matrix.md](references/genre-matrix.md) 专项清单。

报告"题材识别"行写明：`主类型X（得分，权重%）+ 辅类型Y（权重%）｜频道｜已加载模板：文件列表`。
用户在记忆文档中已声明题材的以声明为准，识别仅作校验；声明与文本严重不符时提示"题材漂移"。

### 第3步：规则包加载 + 外部技能调度（v3.0：规则已离线内置）

审稿不是单打独斗。v3.0 起九大技能的核心规则已**离线内化**至 [references/rules-pack/](references/rules-pack/INDEX.md)，审稿时直接 Read 对应规则包作为检查清单执行，**不再依赖本机是否安装外部技能**：

| 审稿需求 | 内置规则包（主） | 来源技能（报告中标注 `[调度:技能名]`） |
|---------|-----------------|--------------------------------------|
| **f) AI味检查** | [rules-pack/ai-flavor-rules.md](references/rules-pack/ai-flavor-rules.md)（硬证据6类+密度判定25项+25项速检+改写六法） | qu-ai-wei-main、说人话、humanizer-zh-main、novel-audit、番茄去味模块 |
| **c) 对话质量/智斗博弈** | [rules-pack/dialogue-game-rules.md](references/rules-pack/dialogue-game-rules.md)（形式7项+博弈结构+真人化+14类问题词库） | 网文对话博弈大师、social-intelligence、story-review、novel-audit、番茄对话监控 |
| **a) 逻辑结构/b) 人物塑造** | [rules-pack/character-logic-rules.md](references/rules-pack/character-logic-rules.md)（因果链/时间黑洞/利益校准四步/降智判定/读者契约/张力五维） | social-intelligence、novel-audit、plot-tension-review、story-review、番茄 |
| **d) 模板化/套路化** | [rules-pack/template-cliche-rules.md](references/rules-pack/template-cliche-rules.md)（桥段克隆8项+最毒句式+分级词库+21类检测器+台词模板） | oh-story story-review、novel-audit、qu-ai-wei、番茄循环检测 |
| **e) 内容合规/平台门禁** | [rules-pack/platform-gate-rules.md](references/rules-pack/platform-gate-rules.md)（门禁G1-G6+三平台rubric+**合规红线C1-C6**） | 番茄小说写作Skill、story-review rubric；红线细目为本技能自建 |
| 人情世故专项 | [rules-pack/character-logic-rules.md](references/rules-pack/character-logic-rules.md) §3（v3.8.0 已并入原 social-audit-checklist 15 项审计清单） | novel-social-intelligence-skill |
| 伏笔追踪专项（v3.4.0；v3.6.0 扩充公路类） | [rules-pack/foreshadow-judgment-rules.md](references/rules-pack/foreshadow-judgment-rules.md)（六大类 27 小类判定 + 状态机 + 台账字段，v3.6.0 新增 F23-F27 公路场景专属）+ [foreshadow-conflict-rules.md](references/rules-pack/foreshadow-conflict-rules.md)（四类冲突校验 + 回收节奏 + 遗忘预警阈值）+ [foreshadow-topic-templates.md](references/rules-pack/foreshadow-topic-templates.md)（12 类分题材伏笔模板，v3.6.0 新增公路求生·克苏鲁向） | foreshadow-tracker-audit 整合（本技能自建规则包，`foreshadow_base` 可选传入做精度对比） |
| 全维度审计/评分框架 | 基础12维（本文件第4步） | novel-audit 11维加权、AI小说创作引擎v3.0 |
| 题材模板/审稿agent | `references/templates/`（silver 全量内置）+ 社交人格卡/关系账本模板 | silver-novel-skills、social-intelligence |

**加载纪律**：
1. 单章全审：5 个主规则包全部 Read（rules-pack 文件为检查清单，按需引用条目，不全文照抄进报告）；
2. 指定维度审：只加载对应包（"只查AI味"→ai-flavor；"只查对话"→dialogue-game；"只查逻辑/人物"→character-logic；"查套路"→template-cliche；"能发了吗"→platform-gate + 全量）；
3. 量化辅助：AI 味/重复/对话占比/段落均质可跑 `scripts/text_stats.py <章节文件> [--mode ai|repeat|structure|full]`（Python 版）或 `scripts/text_stats.ps1 -Path <章节文件> -Mode full|ai|structure|repeat`（PowerShell 版，Windows 零依赖）取量化证据，脚本命中为线索，最终判定需语境复核（对话层/刻意为之豁免）；
4. 本机已安装对应技能时，报告仍以 `[调度:技能名]` 标注规则来源；未安装时标注 `[内置规则包]`；用户明确要求"用XX技能深度改写"时，建议用户在主对话显式触发该技能；
5. **伏笔专项按档位分片加载（v3.4.0）**：quick 档不加载伏笔规则包（仅用第1步伏笔索引 + D2 基础判定）；full 档加载 foreshadow-judgment-rules + foreshadow-conflict-rules；precise 档三包全载（含 topic-templates 分题材模板，或按主题材只挂对应小节）；大纲审分支命中伏笔需求时加载 judgment + topic-templates 两包做节点规划校验，**保证任何组合下总加载文件数仍遵守既有加载上限**。

### 第4步：执行审查（门禁前置 + 基础12维 + 题材专项 + 向量库比对 + 文风监控）

**第4步·0：发布门禁前置扫描（触发"审稿/能发了吗"必跑，规则见 [rules-pack/platform-gate-rules.md](references/rules-pack/platform-gate-rules.md)）**

门禁为一票否决层，先于 12 维评分执行，结果置顶报告：
- **G0 合规红线**：按 C1 政治 / C2 色情 / C3 暴力 / C4 迷信 / C5 价值观 / C6 平台AI 六组逐项扫描，命中 🔴 直接判"暂缓发布"并引用原文位置；边界情形提示作者按目标平台最新规则自查（AI 审稿不替代平台审核）；
- **G1 字数**（2000-2500 标准章，<2000 🔴 / >3000 🟡）、**G2 对话占比**（>40% 🔴，行数法）、**G3 话疗检测**（BOSS 被三句话说服 🔴）、**G4 战斗章**（攻击<3 次/无代价/话疗解决 🔴）、**G5 循环模式**（连续 3 章结构相似度 >70% 🔴，多章审）、**G6 节奏**（高潮密度 <15% 🔴）；
- 门禁任一 🔴：报告结论最高只能到"暂缓发布"，12 维评分照出但不改变门禁结论。

**六大自动化审查（用户触发"审稿"时全部执行，与基础12维的对应关系）**：

| 用户需求 | 审查动作 | 主规则包 | 对应维度 |
|---------|---------|---------|---------|
| a) 逻辑结构审查 | 因果链逐场景过、时间黑洞>1h、行为-规则一致性、场景跳脱/虚假连接词检索、POV 越界 | character-logic-rules | B1-B4、D2 |
| b) 人物塑造审查 | 利益校准四步链逐决策过筛、降智/送人头特征比对、灵魂铁则+语言指纹向量比对、社交段位匹配、好感度双轴、读者契约四问 | character-logic-rules（含 §3 人情账本审计，v3.8.0 已并入原 social-audit-checklist 15 项清单） | C2、D1 |
| c) 对话质量审查 | 锚定比例/飘对话/独白/提示语形式 7 项 + 博弈结构（轮次推进/话题阶梯/攻防回合/筹码拉锯/权力量化）+ 真人化活人感 6 项 | dialogue-game-rules | C4、D1 |
| d) 内容模板化检查 | 桥段克隆 8 项、最毒句式 ★级、一级/二级套式词、21 类检测器信号、开头同质化（前3章）、章尾总结体、身体部位词频、台词模板 | template-cliche-rules | A2、A3、C1 |
| e) 内容合规审查 | G0 红线 C1-C6 逐项扫描 + G1-G6 平台门禁 | platform-gate-rules | 门禁层 |
| f) AI 味检查 | 真人门检 → 硬证据 6 类 → 密度判定 25 项 → 25 项速检清单（≥5 项判明显）→ text_stats.py 量化 → 语体保护（叙述层严/对话层宽） | ai-flavor-rules | A3、文风联动 |

**基础12维**（所有网文必审，每项独立评分0-10）：

```
A. 文字层
  A1 文字基础      病句/错别字/流水账(连续5句动作句无五感)/形容词堆砌(同处≥3个)/冗余
  A2 重复雷同      高频词(500字≥3次)/同义重复/情绪反应克隆/桥段克隆/叙事循环 [向量库:情绪反应库][规则包:template-cliche T1-T8]
  A3 AI痕迹        硬证据6类+密度25项+25项速检/总结腔/机关枪节奏 [规则包:ai-flavor][调度:qu-ai-wei/说人话/humanizer/番茄]

B. 逻辑层
  B1 情节合理性    动机充分/因果链(场景跳脱/虚假连接词)/上帝之手/读者契约 [临时提取:因果链指纹][规则包:character-logic§1][调度:plot-tension-review]
  B2 时间线        时序/年龄/季节/故事内时钟/时间黑洞(跳跃>1h无交代) [临时提取:事件时间链]
  B3 上下文连贯    设定遗忘/情绪突变/状态承接/行为-规则一致性 [临时提取:事件承接指纹]
  B4 视角一致      POV越界/读心/无预告切换 [silver:pov_checker]

C. 体验层
  C1 节奏爽点      拖沓(≥500字无推进)/爽点密度(1000-2000字≥1)/章末钩子/开篇生死线/张力五维 [规则包:character-logic§1.5+platform-gate G6][调度:plot-tension-review]
  C2 人物塑造      性格一致(OOC)/利益校准四步/降智送人头/社交段位/好感度双轴/语言指纹/成长弧 [临时提取:人物+对白指纹][规则包:character-logic§2][调度:social-intelligence]
  C3 世界观自洽    规则一致/能力代价/势力关系 [临时提取:事实冲突比对]
  C4 对话质量      形式7项(锚定/飘对话/独白/提示语/占比/潜台词/千人一面)+博弈结构(轮次推进/话题阶梯/攻防/拉锯)+真人化 [临时提取:对白指纹][规则包:dialogue-game][调度:对话博弈大师]

D. 专项层
  D1 人情世故      利益>立场>情绪>对错/社交段位/人情账本/话留余地/活人感 [规则包:character-logic§3][调度:social-intelligence]
  D2 连载一致性    数值穿帮/伏笔账(5状态机)/物品技能账/任务账 [v3.9.0:先查entity_index+keyword_index+story_summaries索引层，临时提取本章数值逐项核对，缺失时才回溯正文]
```

**题材专项**：按第2步加载的 silver 题材模板（模板要求的必备元素缺失=问题、模板雷区命中=问题）+ 都市四类矩阵（[references/genre-matrix.md](references/genre-matrix.md)）叠加执行。

**向量库三大比对**（按 [references/review-vector-db.md](references/review-vector-db.md)）：
- 剧情逻辑：本章事件 cause 无前置/事实矛盾/状态不承接 → 判因果断裂或事实冲突，引用库记录出处；
- OOC：违反灵魂铁则、语言指纹漂移、情绪反应克隆（连续≥3次同反应）→ 分级判罚；成长弧方向上的变化进"成长候选"不误判；
- 对白口语化：书面腔、千人一面（同台两人指纹相似度>0.7）、飘对话（≥4轮无锚定）、信息倾倒、偏离个人基线。

**文风监控**（按 [references/style-memory-db.md](references/style-memory-db.md)）：
- 生成本章临时文风指纹，与 `.review-db/style_fingerprint.json` 量化比对（v3.7.0 起文风指纹单文件）：超容差带且无剧情理由 → 🔴/🟡 文风漂移；有剧情正当性（战斗/抒情/新阶段）→ 记录不判罚；
- `taboo_patterns` 命中、叙述声音突变、与锚点样本相似度<0.3 → 告警；漂移与AI腔伴生时与A3联动；
- 样本<3章的建档期只描述不判罚；
- 改写示例必须模仿作者档案声音，不得把作者文字改成 AI 的声音。

### 第5步：库回写与记忆更新建议

审稿完成后执行两类回写：

1. **库回写（v3.7.0 起按三级更新分级执行；向量临时提取、审完即弃不落盘）**：
   - 人物/剧情/对白/数值/社交向量：本章临时提取用于审查，审后不持久化，无回写动作；
   - 新事实/成长演变/文风演变：报告中标记 pending，**请用户确认后**随 /sync-setting 或 /sync-outline 写入 setting.md 对应段落；
   - 文风指纹按 EWMA 融合（建档期0.4/0.6，稳定期0.75/0.25）更新 `style_fingerprint.json`，单章异常值挂起、连续3章同向才确认演变；
   - **伏笔索引回写（v3.4.0，v3.7.0 起唯一台账为 index.json + details/）**：本章新增预埋（planted）登记 FID / 分类 / 锚点 / 计划回收章到 `foreshadow/index.json`；强化（reinforced）/ 半回收（half-closed）/ 回收（closed）仅推进对应 FID 状态字段；超期（overdue）/ 冲突（broken）挂预警标记；回写纪律见 [rules-pack/foreshadow-judgment-rules.md](references/rules-pack/foreshadow-judgment-rules.md) §四；新埋伏笔涉及设定事实的标记 pending，**请用户确认后**转正式条目。
   - **轻量索引维护（v3.9.0，默认轻量级自动执行，全部增量追加）**：
     - `entity_index.json`：本章出场实体 `appear_count`+1 / `last_ch` 更新，新实体追加名片行；
     - `keyword_index.json`：追加本章 8-12 个核心关键词（具名实体 + 事件词）到对应条目；
     - `story_summaries.json`：追加本章摘要（3-5 句 events + chars + tags）到 recent_chapters，超过 10 条时最老一条粗合并进 arc_summaries；
     - 索引字段规范与体积控制规则见 [references/review-vector-db.md](references/review-vector-db.md) §1.8。
2. **记忆文档更新建议**（不擅自代写设定事实，可给更新草稿）：
   - 新角色、新数值、新物品/技能、新伏笔埋设或回收、人物关系变化；
   - 出场满 3 章的配角提示建档（/sync-setting 时正式创建 characters/Cxxx.md）；
   - 与"已确认事实"冲突的——优先判正文BUG，建议改正文而非改记忆；作者确认是设定演进才更新。

### 第6步：输出报告

严格按 [references/report-template.md](references/report-template.md) 输出。核心要求：
- 所有问题必须**引用原文具体句子 + 行号/段落位置**，禁止泛泛而谈
- 每个问题附三段式改写：`❌原文 → ✅建议 → 💡思路`
- 评分给出依据；问题分级 🔴高优/🟡中优/🟢低优
- 亮点也要指出（审计是辅助不是否定）
- 报告开头输出"记忆底座"摘要：引用了哪些记忆文档、库实例状态（新建/已有）、核对了哪些数值/伏笔、加载了哪些模板、调度了哪些技能
- 文风维度输出：`🎨 文风指纹：偏离项 X/Y，最大偏离=XX（剧情理由/无理由）；漂移判罚 N 处`

**统一输出格式**：严格按 [references/report-template.md](references/report-template.md) 输出，结构骨架：
```
【整体评分】X/10 → 【核心问题】（通用+专项标注前缀）→ 【优化建议】→ 【剧情风险预警】→ 【专项审查附录】（命中时显示）
```
**降级规则**：快速审稿省略附录；无项目数据不持久化；未命中专项不显示对应板块。

评分算法完全复用原有 12 维算法基础框架，**专项维度单独计分，不影响总分基准**；专项默认**建议级**时，附录结论以优化方向为主，不改变核心等级。

---

## 六、手动控制指令集

| 指令 | 功能 |
|---|---|
| `/init-review` | 在当前项目初始化完整审稿数据库目录结构 |
| `/full-load` | 全量加载所有模块数据，用于全文复盘、整体排查 |
| `/light-mode` | 精简模式，仅加载核心设定，极致压缩 token 消耗 |
| `/sync-setting` | **标准级更新**，同步修改对应设定文档（setting.md + project_state.json），不碰大纲 |
| `/sync-outline` | **全量级更新**，同步更新章纲、卷纲与全量记忆文档 |
| `/force-archive` | 手动归档已回收伏笔，清理索引体积 |
| `/archive-closed` | 手动归档已回收伏笔与已完结剧情线，清理数据库体积（保留兼容） |
| `/export-foreshadow` | 导出全书完整伏笔追踪表为 Markdown 文件 |

**使用说明**：
- 指令直接输入即可触发（如直接发送 `/init-review`），AI 将执行对应操作并返回确认信息；
- 初次打开项目时，若未手动执行 `/init-review`，首次审稿时会自动初始化最小化目录结构；
- `/full-load` 适用于全文复盘、整体逻辑排查场景，token 消耗较大，日常审稿无需使用；
- `/light-mode` 适用于快速审稿、极致压缩 token 场景，仅加载 setting.md 核心设定，不加载向量库与伏笔详情；
- **`/sync-setting`（v3.7.0 新增）**：标准级更新指令。日常审稿默认只做轻量更新（仅改 meta+伏笔索引+文风指纹），当需要把本章的设定变动（人物成长、世界观补充、数值更新）同步写入 setting.md 和 project_state.json 时，加一句 `/sync-setting` 即可。不修改章纲、卷纲。
- **`/sync-outline`（v3.7.0 新增）**：全量级更新指令。当需要根据正文改动同步更新章纲、卷纲时使用，包含 /sync-setting 的全部内容 + 章纲重写 + 卷纲节点修正 + 全局一致性校验。token 消耗较高，仅在需要联动大纲时使用。
- **`/force-archive`（v3.7.0 新增）**：手动强制归档已回收伏笔到 archived.json，减少日常索引加载量。建议每完成一卷或重大剧情线后执行。
- `/archive-closed` 保留兼容，功能同 `/force-archive`；
- `/export-foreshadow` 导出的 Markdown 文件可作为作者备份或编辑参考，不影响数据库原始数据。

> **默认更新原则（v3.7.0）**：普通审稿/改文默认执行**轻量级更新**，只更新 meta.json 当前章节、涉及的伏笔状态、文风指纹，绝不自动修改设定文档和大纲。需要同步设定时加 `/sync-setting`，需要同步大纲时加 `/sync-outline`。

---

## 七、全局资源索引（按需加载）

所有通用规则与模板存放于 `references/` 目录，按需读取。核心文件清单：

- **记忆与分类**：[memory-index.md](references/memory-index.md)（记忆文档索引）、[genre-classifier.md](references/genre-classifier.md)（题材智能识别）、[genre-matrix.md](references/genre-matrix.md)（都市题材矩阵）
- **数据库规范**：[review-vector-db.md](references/review-vector-db.md)（审稿向量库）、[style-memory-db.md](references/style-memory-db.md)（文风记忆库）
- **规则包** `rules-pack/`（详见 [INDEX.md](references/rules-pack/INDEX.md)）：
  - 五大主规则：ai-flavor / dialogue-game / character-logic / template-cliche / platform-gate
  - 专项规则：system-novel（21类赛道）、gender-transition（性转）、foreshadow 三包（判定/冲突/分题材）
  - 辅助：outline-review（大纲审稿）
- **模板库** `templates/`：男频13题材 / 女频8题材 / 13个审稿Agent / 写作技法6模块 / 通用评测流程（含男女频标记）
- **报告模板**：[report-template.md](references/report-template.md)
- **量化脚本**：`scripts/text_stats.py` / `scripts/text_stats.ps1`

**使用说明**：
1. 将本技能文件夹放入全局技能目录（Win: `%userprofile%/.trae/skills`；Mac/Linux: `~/.trae/skills`）即全局生效
2. 打开小说项目文件夹，触发审稿指令，自动在项目根目录创建 `.review-db` 数据库
3. 同一项目跨会话自动读取历史数据，多本书数据完全隔离互不干扰

---

## 三、大纲审稿专项流程（v3.2 新增，与正文六步平行）

**输入为大纲/卷纲/章纲/设定集时走本章，不进入第二章六步正文流程。** 细则见 [rules-pack/outline-review-rules.md](references/rules-pack/outline-review-rules.md)，系统文叠加 [rules-pack/system-novel-rules.md](references/rules-pack/system-novel-rules.md)。

四步流程（不得跳步）：

1. **前置识别**：只读核心设定 + 开篇卷纲/前 3 章章纲，提取①基础分类（频道/篇幅/赛道）②具体题材（精准到细分类型）③整体风格（爽文/写实/轻松/悬疑/慢热）；信息不足给暂定判断并标注。
2. **模板匹配**：按细分题材加载 silver 题材模板（取必备元素/雷区）；命中系统文要素必载 system-novel-rules；都市商战叠加 genre-matrix；单次加载 ≤4 个文件。
3. **专项审查**（两类都做）：
   - **基础逻辑审查**：设定自洽性（规则/数值/金手指）、剧情节奏（黄金三章钩子/主线清晰度/卷间递进）、人物弧光（主角成长线/反派动机/配角目标）、伏笔合理性（核心伏笔埋设与回收计划）；
   - **前瞻风险审查**（v3.2 核心）：推演中期→大后期 3-5 个关键节点，预判剧情崩坏、数值膨胀、人设 OOC、后期写崩（金手指过早封顶/悬念揭露时机/烂尾风险）；系统文按 system-novel-rules 四维度逐项核对，并按 **21 类赛道模板（机制；v3.6.0 新增 #21 公路求生·克苏鲁向）+ 12 套性格人设模板（人设）双校验**，推演 **30 章 / 100 章 / 大结局** 三个节点，输出【系统机制评分】【系统人设评分】【中期风险预警】【优化建议】四模块（v3.2.1）；公路求生·克苏鲁向题材追加节点三要素/资源守恒/SAN 分级/移动性保持前瞻项（v3.6.0，见 system-novel-rules #21 与 character-logic §1.6/§2.6）。
4. **分层输出**：🔴核心问题（必改，不改会崩/流失读者）→ 🟡优化建议（可选项）→ 🔵前瞻预警（当前无问题、后期大概率出事，须写明推演节点+触发条件+规避方向）→ ✅亮点（≥2 条）。报告开头写明审稿前提（分类/题材/风格、加载模板、审到哪一卷章）。

章纲输入追加细化清单：本章目标、冲突密度、章末钩子、单章信息投放 ≤3 个新设定、与卷纲对齐；系统文章节核对任务节奏与奖励匹配，并核本章系统言行是否符合其性格人设（对照 12 套性格模板防 OOC 突变，v3.2.1）。命中性别转变要素时，额外追加「大纲 × 性转阶段规划校验三要点（v3.3.1）」：
- 检查转变五阶段（冲击 → 抗拒 → 适应 → 接纳 → 整合）**在大纲中是否有明确节点**，避免漏阶段或跳步；
- 检查**变嫁情感线与主线剧情的节奏匹配度**：情感线必须在「适应期」之后开启，禁止与身份接纳顺序颠倒；
- 检查**中期、后期的身份转变伏笔**是否合理（暴露 → 被发现 → 重构节点），避免中后期完全不提性别转变影响。

遇到「系统 + 性转」组合题材时，并行加载「系统文模板」+「性转专项模板」交叉校验（v3.3.1）：
- 系统任务节奏 / 奖惩机制 / 数值面板 / 登场时机 → 走 system-novel-rules 四大维度与 21 类赛道模板（v3.6.0 公路求生·克苏鲁向为 #21）；
- 人物的心理 / 行为 / 生理 / 社会身份转变过程 → 走 gender-transition-rules 六大维度与 6 套过程模板；
- **双重校验**：额外输出一句话结论「系统任务节奏与人物转变节奏匹配度：高/中/低 + 失配节点」，低匹配度自动列为前瞻风险。

大纲审 × 伏笔追踪专项联动（v3.4.0，自动叠加，无需用户触发）：大纲/卷纲/章纲审稿时自动执行「伏笔节点规划校验」（细则见 [rules-pack/foreshadow-topic-templates.md](references/rules-pack/foreshadow-topic-templates.md) §四）：
- 校验大纲中**伏笔节点规划**：埋设点与回收点是否明确标注（章纲级），只有埋没回收 → 🟡 回收计划缺失，提示补规划；
- 校验**回收节奏**：大纲规划的回收间距是否符合分题材节奏表（短 5-15 章 / 中 30-60 章 / 长线 ≤150 章且每 20-30 章强化）；
- 校验**长线布局合理性**：跨卷长线伏笔是否有中途强化节点、大结局级伏笔是否在最后 20 章前启动回收铺垫；
- 校验**伏笔负载**：按大纲推算全书 open 伏笔峰值是否 > 20 条（膨胀预警前置到大纲阶段）；
- 与「系统 + 伏笔」「性转 + 伏笔」组合叠加时：系统文大纲自动核对任务 / 奖励 / 系统升级伏笔的前后一致性（对照系统机制四维度），性转大纲自动核对身份 / 情感 / 秘密伏笔与转变阶段规划（暴露→被发现→重构）的埋线节奏匹配；结论分别输出，问题统一汇总。

合规审联动（v3.3.1）：性转场景的平台合规判定完全复用原有 C1-C6 红线清单与合规模块；gender-transition-rules 仅补充性转题材专属风险点（如过度露骨的生理描写、未成年人、性暗示福利化、价值导向问题），**合规定论仍由平台合规模块统一输出，性转专项不单独做合规结论**。

**铁则**：大纲审稿**只提建议、不改原文**——绝不直接改写大纲内容、不代写设定和剧情；所有问题引用大纲原文位置，前瞻预警必须挂推演节点；不超输入范围下死结论。

---

## 四、审查模式

| 模式 | 触发 | 执行范围 |
|------|------|---------|
| 单章全审 | "审这章" | 门禁前置 + 12维 + 题材专项 + 向量库比对 + 文风监控 + 六大审查(a-f)，详细报告 |
| 多章对比审 | "审这几章/前后连贯吗" | 额外加跨章一致性（数值曲线/伏笔时间线/关系演进/重复桥段克隆 T1-T8/叙事循环 G5/文风演变曲线） |
| 指定维度审 | "只查AI味/只查OOC/只查数值/只查文风/只查对话/查套路/查合规" | 聚焦对应规则包与维度，其余跳过 |
| 发布前门禁审 | "能发了吗/过一遍平台规则" | G0 合规红线 + G1-G6 门禁 + 12维，输出发布/暂缓结论 |
| 大纲专项审（v3.2） | "审大纲/把下卷纲/看看章纲/设定集有没有问题" | 第三章四步流程：基础逻辑审查 + 前瞻风险预警 + 三级建议输出；不跑门禁/12维/向量库 |
| 系统文专项（v3.2.1；v3.6.0 扩充公路赛道） | "系统设定行不行/签到机制/任务奖励/会不会数值膨胀/系统人设崩没崩/类型题材违和吗/公路求生载具升级合理吗/燃油资源守恒吗/行车规则有没有崩" | system-novel-rules 四维度 + 21 类赛道模板（v3.6.0 新增 #21 公路求生·克苏鲁向，与 #4 末世、#16 诡异克苏鲁三重叠加）+ 12 套性格人设模板；大纲阶段做机制/人设双校验 + 30/100/大结局前瞻推演，正文阶段联动 C2/C3/D2（系统 OOC 判 C2；公路题材另联动 C1 节点节奏与 SAN 分级行为校验） |
| 性别转变专项（v3.3.0 / v3.3.1 兼容升级） | "性转设定合不合理/男变女心理崩不崩/变嫁有没有铺垫/女装大佬违和/性别转变会不会 OOC" + 手动开关「启用性转专项审查 / 禁用性转专项，按通用标准审」 | gender-transition-rules 六大维度 + 4 类模式标准 + 2 类方向 + 8 类小众赛道 + 6 套转变过程模板；**双重触发校验 + <70% 自动降级 + 建议级默认灰度**；大纲阶段追加阶段规划校验三要点、系统 + 性转交叉校验（系统任务节奏 vs 人物转变节奏匹配度）；正文阶段联动 C1/C2/C4，强制型性转叠加系统文专项；命中时在原报告末尾追加【专项审查附录】四子段（转变节奏/心理逻辑/行为细节评分 + 核心人设一致性校验），专项单独计分不影响总分 |
| 伏笔追踪专项（v3.4.0；v3.6.0 扩充公路类） | "查伏笔/排查伏笔/伏笔检查/回收核对/剧情复盘/伏笔忘收/剧情吃书/前后矛盾/线索遗漏/埋线/挖坑/填坑/闭环/路牌伏笔/收音机怪谈回收了吗" + 手动开关「启用伏笔专项深度审查 / 禁用伏笔专项，按通用标准审」 | foreshadow 三规则包：六大类 27 小类判定（v3.6.0 新增 F23-F27 公路场景专属：标识/载具/道路/节点/规则）+ 全生命周期状态机（预埋→跟踪→回收→冲突校验）+ 四类冲突校验 + 遗忘预警阈值 + 12 类分题材伏笔模板（v3.6.0 新增公路求生·克苏鲁向）+ foreshadow/index.json 索引 + details/ 详情（跨章节自动继承，v3.7.0 起唯一台账）+ foreshadow_base 可选精度对比；**深度三档 quick/full/precise 直接继承审稿深度**；正文审自动基础伏笔检查（未命中时不显示附录、输出与原版一致），大纲审自动叠加节点规划校验；公路题材下 F23-F27 与 F20-F22 同为高精度识别重点；可与系统文/性转/大纲专项任意叠加，结论分别输出、问题统一汇总；命中时在【专项审查附录】下追加伏笔子段（本章新增预埋/全局未回收/逻辑冲突/精度对比/全书追踪表），专项默认建议级不影响总分 |

---

## 五、铁律

1. **记忆文档是最高事实源**。正文与 setting.md 已确认事实/伏笔索引冲突时，判正文BUG；作者明确声明改设定的除外。
2. **"刻意为之"不算BUG**。角色口癖、故意的文风、记忆文档里写明的反差设计、作者声明的锚点风格，不得误判。
3. **AI味判定看密度不看单次出现**。排比、四字词、情绪词单次出现不判，短段反复堆叠才判。
4. **不擅自续写、不大幅改写**。只给可落地的修改建议和示例句，尊重原作剧情走向；改写示例必须用作者自己的文风声音。
5. **数值/伏笔判断必须有出处**。每条穿帮指控写明"记忆文档/伏笔索引第X条记录为A，正文第Y段写为B"。
6. **项目数据严格隔离**。只读写当前项目 `.review-db/`，技能目录本身不存任何项目数据；库缺失自动新建，串库立即停止。
7. **防漂移不防进化**。文风监控有容差带；有剧情支撑的连续风格演变确认后更新档案，不把作者锁死在第一章的声音里。
8. **外部规则冲突仲裁**：记忆文档事实 > 作者声明 > 平台规则 > 模板/技能方法论 > 通用写作规律。性转专项进一步遵循「**通用规则优先级高于专项规则**」：通用规则（九大核心规则包）与专项规则无交集时各自独立判定；判定重叠时以通用结论为准；通用规则已判定为「问题」的内容，性转专项不再重复判定，避免双重扣分。
9. **大纲/设定只提建议不改原文**（v3.2）。大纲审稿仅输出问题诊断、风险提示与优化方向，绝不直接改写大纲内容、不代写设定和剧情；前瞻预警必须挂推演节点，不超输入范围下死结论。
10. **专项通用铁律（性转/伏笔/公路克苏鲁，v3.3.1~v3.6.0）**：① 默认**建议级**灰度运行，不影响核心总分与审稿速度；稳定后可升级为问题级。② 专项数据物理隔离（存独立子目录），不污染通用库；③ 冲突仲裁遵循「通用规则优先级高于专项」，无交集时各自独立判定，重叠时以通用结论为准，避免双重扣分；④ 专项诊断只提建议（优化方向/时机/方式调整），不擅自改写原文；⑤ 模块级回退：删对应规则文件 → 删 INDEX 条目 → 删 SKILL 对应章节，立即回到上一版本功能集，核心功能不受影响。

---

## 附录：审稿意图词典（v3.1 新增，持续更新）

第〇章语义补全时对照本词典识别意图。关键词命中即映射到对应审稿能力；多类同时命中时全部纳入任务单【核心维度】；新词可持续向本附录补充，不影响核心审稿逻辑。

### 人物类 → 人物逻辑审查（OOC / b 项 / C2）
OOC、ooc、人设崩、人设崩了、人物不对、人物不对劲、不符合人设、角色行为奇怪、性格崩了、崩人设、ooc了吗、符合人设吗、人物立不住、降智、送人头

### 文本质量类 → AI 味检测（f 项 / A3）+ 模板套路检测（d 项）
AI味、AI味重、太假了、不像人写的、生硬、模板化、套路、太套路了、老套、有没有新意、水字数、文笔差、读着尬、尴尬、不自然、机器味、一股AI味、去味、去AI味

### 剧情类 → 剧情逻辑审查（a 项 / B1-B3、C1、D2）
逻辑不通、说不通、伏笔穿帮、伏笔忘了、数值崩了、数值对不上、节奏慢、拖沓、没张力、太平淡、节奏有问题、爽点不够、剧情崩了、接不上、前后矛盾、上帝之手

### 对话类 → 对话优化（c 项 / C4）
对话假、台词尬、台词尴尬、不像真人、对话没张力、博弈感弱、对话水、话太多、千人一面、说的话不像他、改对话、对话优化

### 人情世故 / 商战类 → 人情世故校准（D1）
商战假、不符合职场、人情世故不对、不懂人情世故、社交尴尬、利益不对、商战、职场假、官场不对、人设有没有社会经验

### 合规类 → 合规审查（e 项 / 门禁 G0-G6）
违规吗、能过审吗、能过吗、番茄能过吗、起点能过吗、敏感内容、查敏感、查红线、屏蔽词、违禁、能不能发、能发了吗、过审

### 文风类 → 文风校验（style 库 / A3 联动）
文风飘了、风格不对、风格变了、文风漂移、不像同一个人写的、保持人设（指叙述风格）、文笔风格

### 大纲 / 规划文档类 → 大纲审稿分支（outline-review-rules，第三章）
大纲、总纲、卷纲、章纲、细纲、章节大纲、设定集、世界观、开篇设定、剧情框架、故事梗概、把下大纲、大纲行不行、大纲有没有问题、帮我看下大纲、金手指设定、设定有没有漏洞

### 系统文专项类 → 系统文规则（system-novel-rules；大纲阶段走大纲分支双校验，正文联动 C2/C3/D2）
系统设定、系统规则、系统流、金手指、任务奖励、任务机制、签到机制、签到流、神豪流、属性点、加点、商城、面板、数值膨胀、数值崩盘、奖励合不合理、系统什么时候出现、会不会烂尾、诸天、末世系统、种田经营、好感度系统；**v3.2.1 细分赛道**：反派流、掠夺系统、直播流、弹幕系统、词条编辑、设定编辑、马甲系统、身份扮演、轮回回档、死亡回档、学霸系统、知识转化、养崽、宿主养成、克苏鲁、诡异系统、宗门建设、门派流、美食系统、食神、山贼流、军阀争霸、争霸系统、赘婿、上门龙婿、签到商城之外的系统赛道设定；**系统人设/一致性**：系统人设、系统 OOC、系统崩人设、系统抢戏、系统语气突变、系统类型匹配、系统和题材违和、系统性格模板

### 性别转变专项类 → 性别转变规则（gender-transition-rules；大纲叠加系统文/分支，正文联动 C1/C2/C4；v3.3.1 兼容升级：双重触发 + <70% 自动降级 + 建议级默认灰度）
**通用类**：性转、性别转换、变身、男变女、女变男、TS、性转文；**细分类**：女装大佬、伪娘、男娘、变嫁、嫁人、女体化、男体化；**剧情类**：一觉醒来变了、系统强制变身、诅咒变性、慢慢变成女生、慢慢变成男生；**专项审类**：性转设定合不合理、男变女心理、女变男违和、变嫁铺垫、女装大佬违和感、性转人设 OOC、转变过程、身份暴露、婚身份重构、强制型性转、渐变式性转、硬转、修仙古风职场末世娱乐圈校园反派重生性转；**手动开关类**：启用性转专项审查、禁用性转专项，按通用标准审

> **编写纪律（v3.3.1 Trae Skill 原生兼容）**：性转相关触发词统一追加到本节既有「审稿意图词典」，不新建独立词典、不另行分散存放，便于后续扩充与审计。

### 伏笔追踪专项类 → 伏笔追踪专项（foreshadow-judgment + conflict + topic-templates；正文联动 D2 伏笔索引 + 第1步索引读取，大纲分支叠加节点规划校验；v3.4.0：关键词激活深度档 + 自动叠加基础检查 + 深度三档继承审稿深度）
**通用类**：伏笔、埋线、埋设、挖坑、填坑、回收、回收核对、线索、线索遗漏、闭环、剧情呼应、伏笔检查、伏笔追踪；**动作类**：查伏笔、排查伏笔、剧情复盘、伏笔回收核对、挖坑填坑检查；**问题类**：伏笔忘收、伏笔忘了、剧情吃书、吃书、前后矛盾、埋了不收、坑没填；**深度档类**：伏笔全链路、全书伏笔、伏笔台账、伏笔追踪表；**手动开关类**：启用伏笔专项深度审查、禁用伏笔专项，按通用标准审；**可选参数类**：foreshadow_base（原生系统伏笔输出传入做精度对比，可不传）

> **编写纪律（v3.4.0 Trae Skill 原生兼容，同 v3.3.1 惯例）**：伏笔相关触发词统一追加到本节既有「审稿意图词典」，不新建独立词典、不另行分散存放；`foreshadow_base` 为可选输入参数，用户传入时执行精度对比、未传入自动跳过，不构成必填门槛。

### 公路求生·克苏鲁向类（v3.6.0 新增）→ 三重叠加调度（system-novel-rules #21 赛道模板 + foreshadow F23-F27 公路场景伏笔 + character-logic §1.6 节点节奏 / §2.6 SAN 分级行为校验；不新增专项层，属既有专项的题材内容增量，门控规则见铁律第 10 条专项通用铁律）
**题材类**：公路求生、末日公路、末世公路、诡异公路、克苏鲁公路、公路文、公路流、行车规则、载具求生、车载求生、移动求生、公路节点、服务区求生、隧道求生、加油站怪谈、线性公路、公路推进；**元素类**：SAN 值、san 值、理智值、掉 SAN、SAN 值衰减、认知污染、路牌诡异、异常路牌、里程标、里程标重复、指示牌涂改、不存在的出口、服务区事件、隧道异常、隧道回声、收音机怪谈、车载收音机、诡异频段、后视镜影子、车辆自行启动、仪表盘异常、废弃加油站、加油站编号、行车守则、夜间行车禁令、午夜停车、载具升级、装甲车改造、燃油消耗、资源守恒、公路规则、循环公路、永远走不完的直道、被公路同化、公路真相；**专审类**：载具升级合不合理、燃油够不够、资源守恒吗、节点节奏对不对、SAN 值掉得合理吗、掉 SAN 为什么没影响、行车规则有没有崩、公路伏笔回收了吗、是不是变成公路打怪文、有没有长期停滞建基地

> **编写纪律（v3.6.0 Trae Skill 原生兼容，同 v3.4.0 惯例）**：公路求生·克苏鲁向触发词统一追加到本节既有「审稿意图词典」，不新建独立词典、不另行分散存放；命中「公路/载具/行车 + 求生/末世/系统 + 克苏鲁/诡异/SAN」要素组合时按三重叠加调度（系统文 #21 与 #4/#16 叠加 + 伏笔第六大类高精度识别 + 人物逻辑 §1.6/§2.6 门控激活），仅命中单一要素（如只说"载具"无求生/诡异语境）不强制激活，未命中时全部规则休眠、输出与 v3.5.0 完全一致。

### 通用类 → 全维度审稿
审一下、审审、看看、帮我看看、把关、检查、查一下、校一遍、校对、改一下、优化、润色、这章怎么样、质量怎么样、全面检查

### 指令修饰词（调节深度与输出，不单独构成意图）
- 深度：细查、深扒、逐句、好好看看、仔细审、全面点
- 快速：简单看看、简单看下、快速过一下、大概看下、粗略
- 输出：只说问题、别给建议、给打分、打个分、多少分、简要、一句话总结、分点、给修改示例、直接改
- 标准：番茄标准、起点标准、按番茄、按起点、按我的红线、C1-C6
- 兼容（v3.3.1）：置信度<70%、自动降级、建议级、问题级、模块级回退、专项审查附录
- 兼容（v3.4.0）：foreshadow_base、伏笔台账、全书伏笔追踪表、深度档（quick/full/precise）、【伏笔】前缀、【伏笔预警】、伏笔追踪子段、账实分离、模块级回退清单
- 手动控制指令（v3.5.0）：/init-review、/full-load、/light-mode、/archive-closed、/export-foreshadow、初始化数据库、全量加载、精简模式、归档已回收、导出伏笔表
- 兼容（v3.6.0）：公路求生·克苏鲁向、三重叠加、F23-F27、公路场景专属伏笔、公路节点节奏、SAN 分级污染、节点三要素、移动性丧失预警、载具升级梯度
- 手动控制指令（v3.7.0）：/sync-setting、/sync-outline、/force-archive、同步更新设定、同步更新大纲、同步章纲、同步卷纲、强制归档、三级更新、轻量模式、标准级更新、全量级更新
- 兼容（v3.7.0）：三级更新分级机制、记忆文档整合精简、无指令不碰大纲、增量写入、project_state.json、style_fingerprint.json、setting.md 单文件化、临时向量、持久化最小集
